package model;

import java.time.LocalDate;

public class GestoreVisite {

    // public static void creaVisiteMese(LocalDate inizio, LocalDate fine) { 
    //     for(LocalDate giorno = inizio; !giorno.isAfter(fine); giorno = giorno.plusDays(1)){
    //         if(!DatiCondivisi.getDatePrecluse().getElementByKey("1").getDate().contains(giorno)){
    //             for(TipoVisita t : DatiCondivisi.getElencoTipiVisita().getElenco().values()){
    //                 if(Giorni.equalsGiorno(t.getGiorniDisponibili(), giorno)){
    //                     Visita v = new Visita(giorno, t);
    //                     DatiCondivisi.aggiungiVisita(v);
    //                 }
    //             }
    //         }
    //     }
    // }
}